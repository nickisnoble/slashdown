import type { SD } from "./types";
import renderer from "./renderers/json"; // TODO: Switch default to HTML

export class Slashdown {
  constructor( src: string = "", renderer: SD.Renderer = renderer ) {

  }
}